import 'dart:io';

import 'package:flutter/material.dart';

class Res extends StatelessWidget {
  const Res({super.key, required this.image1, required this.image2});
  final File image1;
  final File image2;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}